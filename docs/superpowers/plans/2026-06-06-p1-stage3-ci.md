# P1 — Stage 3: CI/CD Hardening Implementation Plan

> Execute task-by-task; each task commits. Steps use `- [ ]`.

**Goal:** Replace the single broken `ci.yml` (references pre-restructure `frontend/`, `src/`) with a monorepo, multi-job pipeline + supply-chain scanning + a tagged multi-arch release — using **honest incremental enforcement**: required jobs are the ones green today; pre-existing debt runs **advisory** (visible, non-blocking) and gets flipped to required as P2/P3/Stage-cleanup clear it. No source changes.

**Enforcement matrix:**
| Job | Status | Why |
|---|---|---|
| `build` (turbo build) | **required** | green today (api/web/shared) |
| `test` (api + worker via turbo filter) | **required** | 425/20 + 37 green today |
| `secret-scan` (gitleaks) | **required** | must stay clean |
| `compose-validate` | **required** | config valid today |
| `quality` (typecheck, lint, web-test, coverage) | **advisory** | pre-existing debt (api tsc ~53, web vue-tsc 33, web 64 fails) — flip per phase |
| CodeQL | reports to Security tab | SAST |

**Deferred (noted, not built now):** changesets versioning workflow (needs team flow), Trivy image scan (→ release pipeline / P8). Renovate used for deps (better bun support than Dependabot).

**Caveats:** gitleaks-action is free for **public** repos (private needs `GITLEAKS_LICENSE`). Renovate needs the Renovate GitHub App installed. Bun pinned to 1.2.2.

---

## Task 1: Rewrite `.github/workflows/ci.yml` + `.gitleaks.toml`
- [ ] Write `ci.yml` (jobs: build, test, quality[advisory], secret-scan, compose-validate) — see file.
- [ ] Write `.gitleaks.toml` allowlisting `*.db*` (some local DBs sit in early history; binary, not real secrets).
- [ ] Verify YAML parses; verify the commands the jobs run pass locally (`bunx turbo run build`, `turbo run test --filter`).
- [ ] Commit.

## Task 2: `.github/workflows/codeql.yml`
- [ ] CodeQL for `javascript-typescript`, on push/PR to main + weekly cron. Commit.

## Task 3: `.github/workflows/release.yml`
- [ ] On `v*` tags: buildx multi-arch (amd64+arm64) → push `ghcr.io/<repo>` (target `api`), with semver tags + `latest`, gha cache. Commit.

## Task 4: `renovate.json`
- [ ] `config:recommended`, weekly schedule. Commit.

## Self-Review
- Required jobs = exactly what's green today (no fake-green). Advisory jobs surface debt without blocking; documented to flip per phase.
- No source touched; the 425/20 + 37 suites and `turbo build` are what `test`/`build` run — already verified green this session.
